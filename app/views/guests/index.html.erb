<div class="container guests">
  <h1 class="display-1 text-center guests-title">Will you join the party?</h1>

  <div class="row">
    <div class="col">
      <div class="guest-deck">
        <% @guests.each do |guest| %>
          <div class="card guest-card">
            <%= image_tag "LOTR/Legolas.png", class: "card-img-top", alt: "Legolas" %>
            <div class="card-body">
              <h5 class="card-title"><%= "#{guest.name} #{guest.last_name}" %></h5>
            </div>
            <div class="card-footer">
              <% if guest.participate %>
                <%= content_tag :button, 'EDIT', data: {toggle: "modal", target: "#guestmodal-#{guest.id}"}, class: "btn btn-sm btn-outline-primary btn-guest" %>
              <% else %>
                <%= link_to "YES", guest_participate_path(guest, participate: true), method: :patch, class: "btn btn-sm btn-outline-primary btn-guest", data: {toggle: "modal", target: "#guestmodal"}, remote: true %>
              <% end %>
              <%= link_to "NO", guest_participate_path(guest, participate: false), method: :patch, class: "btn btn-sm btn-outline-secondary btn-guest", remote: true %>
            </div>
          </div>
          <div class="modal fade" id="guestmodal-<%= guest.id %>" tabindex="-1" role="dialog" aria-labelledby="timelineModal" aria-hidden="true" data-backdrop="static">
            <div class="modal-dialog modal-dialog-centered" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <h5 class="modal-title" id="exampleModalLabel">Edit RSVP info</h5>
                  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                  </button>
                </div>
                <div class="modal-body">
                  <%= render "edit", guest: guest %>
                </div>
              </div>
            </div>
          </div>
        <% end %>

      </div>
    </div>
  </div>

</div>
